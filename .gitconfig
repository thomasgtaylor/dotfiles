[alias]
    amend = commit --amend --no-edit
    co = checkout
    s = status
    unadd = reset
    uncommit = reset HEAD^
[core]
    editor = nvim
[diff]
    noprefix = true
[pager]
    branch = false
